# What the design system's scripts need, installed by setup.sh / setup.ps1 into
# ~/.wwtdigital-design/venv. The static lint and the teardown need none of these.
playwright>=1.45
pillow>=10.0
numpy>=1.26
fonttools>=4.50
brotli>=1.1
python-pptx>=0.6.23
